# value-fuel-by-distance
Experimental project to provide insights into best value fuel accounting for travel costs to cheaper fuel price

```bash
brew install python-tk@3.14
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install -r requirements.txt
python3 -m pip install --upgrade pip
```